<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
	<mapper namespace="board.model.Board">
		<insert id="insertBoard">
			insert into board(num,name,email,subject,content,pw,reg_date,ck) 
			values(boardseq.nextval, #{name}, #{email}, #{subject}, #{content}, 
					#{pw}, sysdate, '0')
		</insert>
		
		<select id="getContactByEmail" resultType="board.model.BoardBean">
			select * from board where email =#{email}
		</select>
	</mapper>